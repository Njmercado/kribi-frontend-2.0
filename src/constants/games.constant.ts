import { IGame } from "../interfaces";

const GAMES: Array<IGame> = [
  { name: 'Juego de construir palabras', description: 'Selecciona la carta que traduzca a una de las palabras listadas', id: 'BuildWordsGame' },
]

export default GAMES;